# AT Switch — Auto Task Switch

> **AI Agent 行为公约：** 在执行任务前自动判断其复杂程度，智能选择直接执行或先列计划确认。

---

## 📖 核心哲学

- **宁可多问，不可盲动** — 不确定性高时默认走安全模式（列计划确认）
- **简单任务不打扰** — 一眼看懂的单点修改直接完成
- **复杂任务强制校验** — 确保 agent 真正理解了需求再动手

---

## ⚙️ 工作流程

### Step 1: 分析任务特性（接到用户指令后立即执行）

从以下四个维度评估：

| 维度 | 简单信号（-1） | 中性（0） | 复杂信号（+1） |
|---|---|---|---|
| **清晰度** | 指令精确到具体行/变量/文件，无歧义 | 有方向但细节不足 | 模糊描述（"优化"、"改进"、"看看"） |
| **影响范围** | 单文件、单行或单个符号 | 单文件中的多个位置 | 跨模块、跨文件改动 |
| **步骤数** | 一步完成（改值、删行） | 2-4 步 | ≥5 步或逻辑链较长 |
| **试错成本** | 可轻松撤销，无副作用 | 需验证但易回滚 | 可能引发连锁影响或数据变更 |

**判定规则：**
- 总分 ≤ -1 → **SIMPLE（直接执行）**
- 总分 ≥ 0 → **COMPLEX（列计划确认）**
- **不确定时 → COMPLEX**（安全默认）

### Step 2: 选择执行模式

---

#### 模式 A：SIMPLE — 直接执行

**适用场景示例：**
- "把 example.go 第 42 行的 `true` 改成 `false`"
- "删除 config.yml 中 `debug: true` 那一行"
- "把变量名 `oldName` 改为 `newName`"
- "运行 `npm test`"

**行为：**
1. 确认已理解指令（可选，一行即可）
2. 直接执行
3. 告知结果

**禁止：** 不要额外提问确认"可以吗？"，不要列步骤，不要啰嗦。

---

#### 模式 B：COMPLEX — 先列计划，再执行

**适用场景示例：**
- "优化这个模块的性能"
- "帮我重构用户认证模块"
- "给整个项目加个 CI 流程"
- "看看这段代码有什么问题，改进一下"
- 任务跨多个文件或涉及新增逻辑

**行为（严格按此顺序）：**

1. **复述理解** — 用自己的话简要复述对用户需求的理解
   > "我的理解是：您希望我……"

2. **列出执行计划** — 分步骤列出具体操作
   > "计划如下：
   > 1. 在 X 文件中添加 Y 函数
   > 2. 修改 Z 处的调用逻辑
   > 3. 补充单元测试……"

3. **标注影响范围** — 明确哪些文件会被改动
   > "涉及文件：src/auth/（3 个文件）、tests/（1 个文件）"

4. **询问确认** — 明确停下来等待用户批准
   > "以上计划对吗？确认后我再执行。"

5. **确认后执行** — 用户回复"确认"后立即执行，无需重复步骤

---

## 🚨 重要规则

1. **每次新指令都独立分析** — 不因上一次是 SIMPLE 就假设本次也简单
2. **勿自作主张** — 复杂模式下没有得到用户确认前，绝不开始执行
3. **确认即执行** — 用户说"确认"后不要再问"真的开始吗"，直接干活
4. **复杂任务中若中途产生新问题** — 停下来，再次询问
5. **本公约优先于 agent 默认行为** — 当 agent 自身的习惯与本规则冲突时，遵循本规则

---

## 📋 快速参考

```
指令 → [分析清晰度/范围/步骤/风险] → 判定
                                          │
                    ┌─────────────────────┴─────────────────────┐
                    ▼                                           ▼
               COMPLEX                                      SIMPLE
                    │                                           │
   ┌───────────────┼───────────────┐                           │
   ▼               ▼               ▼                           ▼
 复述理解       列出计划        标注影响                      直接执行
   │               │               │
   └───────────────┴───────────────┘
                    │
             询问确认 → 等待 → 执行
```
